@model DangBoWeb.Models.LoaiCongVan
@{
    ViewBag.Title = "Create";
}
@using (Html.BeginForm("Create", "LoaiCongVan", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="card shadow mb-4">
        <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
            <h6 class="m-0 font-weight-bold text-primary">Thêm Loại Công Văn</h6>
            <a href="@Url.Action("Index", "LoaiCongVan")" class="btn btn-danger">
                Hủy
            </a>
        </div>

        <div class="card-body">
            <div class="user">
                <div class="form-group">
                    <input type="text" class="form-control form-control-user" id="LoaiCV" name="LoaiCV" placeholder="@Html.DisplayNameFor(model=>model.LoaiCV)">
                    <span>@Html.ValidationMessageFor(model => model.LoaiCV, "", new { @class = "text-danger" })</span>
                </div>
                <div class="form-group">
                    <input type="text" class="form-control form-control-user" id="KyHieu" name="KyHieu" placeholder="@Html.DisplayNameFor(model=>model.KyHieu)">
                    <span>@Html.ValidationMessageFor(model => model.KyHieu, "", new { @class = "text-danger" })</span>
                </div>
            </div>
            <div class="text-center">
                <input type="submit" value="Lưu" class="btn btn-success" />
            </div>
        </div>
    </div>
}

